// Platform URL — set this to the actual app URL when ready.
// Used for Sign In, Sign Up, and other links pointing to the platform.
export const PLATFORM_URL = '';

export const PLATFORM_SIGNIN_URL = PLATFORM_URL ? `${PLATFORM_URL}/signin` : '#';
export const PLATFORM_SIGNUP_URL = PLATFORM_URL ? `${PLATFORM_URL}/signup` : '#';
